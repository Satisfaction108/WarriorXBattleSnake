services:
  - type: web
    name: warriorx-battlesnake
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PORT
        value: 10000

